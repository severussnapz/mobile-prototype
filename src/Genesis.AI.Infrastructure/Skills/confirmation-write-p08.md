# SKILL: confirmation-write-p08
# Phase: P08 Security — Phase 5

## Confirmation and Write — P08

> 📝 **WRITE PROTOCOL — MANDATORY:** For each requirement, write the Security section to the REQ file **one at a time**. After writing: log `"✅ REQ-{N} Security section written ({M}/{TOTAL} complete). Moving to REQ-{N+1}."` Then discard from working context before processing the next requirement.

### What to Write

Write `## Security (Added by Pipeline 08)` section to each requirement file.

Use the `security-check-authoring` skill template.

### After Writing

1. Update `feedback/P08_REVIEW_LIST.md` — mark Written as ✅.
2. Update the attack vector checklist to show all items resolved.

### No Placeholders

See `no-placeholder-enforcement` skill. Never write TBD or placeholder values.
